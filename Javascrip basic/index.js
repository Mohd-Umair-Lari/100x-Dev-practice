function sum(a,b){
    return a + b;
}

let ans=sum(5, 10);
console.log(ans);
// this is a sinppet of synchronous code each of the statements will be executed one after another in a single thread, and in ordered way.